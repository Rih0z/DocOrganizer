# 右側プレビュー問題 - 根本原因確定報告

## 🚨 根本原因判明

### 強化デバッグログ分析結果
- **期待されるログ**: `[緊急デバッグ]` `[右側プレビューデバッグ]` - **出力されず**
- **実際のログ**: `[PIXEL_RAW]` のみ - **V2のImageProcessingServiceから出力**

### 問題の核心
**V3のDragDropHandlerViewModelが V2のImageProcessingService を直接呼び出している**

**問題箇所**: `src/DocOrganizer.UI/ViewModels/V3/DragDropHandlerViewModel.cs:342行目`
```csharp
var pdfDocument = await _imageProcessingService.ConvertImagesToPdfAsync(imageFiles);
```

### アーキテクチャ違反
- **V3設計**: `IFileAdditionService` （V3専用サービス）使用
- **実際の動作**: `IImageProcessingService` （V2レガシーサービス）直接呼び出し

### 影響範囲
1. **右側プレビュー不表示**: V3のMainCompositeViewModelイベントハンドラー群が呼ばれない
2. **ログ出力異常**: V2サービスのログのみ出力、V3デバッグログ無し
3. **PageListBox_SelectionChanged未発火**: 左側サムネイルクリックイベントが動作しない

## 📋 技術分析

### V3アーキテクチャの意図
```csharp
// V3正しい流れ
DragDropHandlerViewModel 
  → IFileAdditionService (V3)
    → NewDocumentCreated event
      → MainCompositeViewModel.OnNewDocumentCreated
        → Pages.Clear() + Pages.Add()
          → PageListBox ItemsSource更新
            → 左側サムネイル表示
              → SelectionChanged発火
                → PreviewManagement.UpdatePreviewAsync
                  → 右側プレビュー表示
```

### 現在の異常な流れ
```csharp
// 現在の異常流れ
DragDropHandlerViewModel 
  → IImageProcessingService (V2) ← **ここが問題**
    → [PIXEL_RAW] ログ出力のみ
    → V3イベント一切発火せず
      → MainCompositeViewModel未通知
        → Pages未更新
          → PageListBox空状態
            → 右側プレビュー表示不可
```

## 🔧 修正方針

### 即座修正: DragDropHandlerViewModel.cs修正
**CreateNewDocumentFromFilesAsync メソッド（342行目）を以下に変更**:

```csharp
// 修正前（V2直接呼び出し）
var pdfDocument = await _imageProcessingService.ConvertImagesToPdfAsync(imageFiles);

// 修正後（V3サービス使用）
var result = await _fileAdditionService.CreateDocumentFromImagesAsync(imageFiles);
var pdfDocument = result.Document;
```

### 前提条件確認
1. **IFileAdditionService.CreateDocumentFromImagesAsync** メソッドの存在確認
2. **FileAdditionService** 内でのV3イベント発火確認

## 📊 検証手順

### 修正後の期待ログ
```
[緊急デバッグ] OnDocumentOpened開始: Document.Pages.Count=1
[緊急デバッグ] Page追加: PageNumber=1, SourceImagePath='Image_20250210_235730_150 (1).jpeg'
[緊急デバッグ] Pages.Count=1
[緊急デバッグ] SelectedPage設定: PageNumber=1
[右側プレビューデバッグ] SelectedPage設定完了: PageNumber=1
[右側プレビューデバッグ] V3ViewModel.PreviewManagement=True
```

### 成功判定基準
1. V3デバッグログの出力確認
2. 左側サムネイル表示確認  
3. PageListBox_SelectionChanged発火確認
4. 右側プレビュー表示確認

## 🎯 結論

右側プレビュー問題の根本原因は**アーキテクチャ違反**：
- V3のDragDropHandlerViewModelがV2のImageProcessingServiceを直接呼び出し
- V3のイベント駆動アーキテクチャが完全にバイパスされている
- MainCompositeViewModelに変更通知が一切届かない

**修正完了後、右側プレビューが正常表示される見込み**。

---
生成日時: 2025-08-18 17:58:00 JST