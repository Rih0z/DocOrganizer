# 右側プレビュー表示機能テスト手順書

## 修正完了事項
- ✅ MainCompositeViewModel: UpdateCurrentPageAsync → UpdatePreviewAsync 修正
- ✅ PreviewManagementViewModel: ImagePath → SourceImagePath 修正  
- ✅ PreviewManagementViewModel: 型不一致修正 (V3PageViewModel)
- ✅ PreviewManagementViewModel: 依存関係フィールド追加修正
- ✅ PreviewManagementViewModel: PreviewUpdatedEventArgs型修正
- ✅ EXE生成完了: C:\Users\217216X721451\github\DocOrganizer\release\DocOrganizer.exe (305,807,761 bytes)

## 最新ビルド情報
- **ファイル**: DocOrganizer.exe
- **サイズ**: 305.8 MB
- **生成日時**: 2025-08-18 13:47
- **場所**: C:\Users\217216X721451\github\DocOrganizer\release\DocOrganizer.exe

## 修正された全ての問題
### 1. メソッド呼び出し修正
```csharp
// 修正前
await PreviewManagement.UpdateCurrentPageAsync(SelectedPage, true);

// 修正後  
await PreviewManagement.UpdatePreviewAsync(SelectedPage, true);
```

### 2. 型不一致修正
```csharp
// 修正前
public async Task UpdatePreviewAsync(PageViewModel? selectedPage, bool forceUpdate = false)

// 修正後
public async Task UpdatePreviewAsync(V3PageViewModel? selectedPage, bool forceUpdate = false)
```

### 3. 依存関係修正
```csharp
// 追加された依存関係フィールド
private readonly IImageLoaderService _imageLoaderService;
private readonly IDialogService _dialogService;
private readonly ILogger<PreviewManagementViewModel> _logger;
private readonly IPdfEditorService _pdfEditorService;
```

### 4. イベント型修正
```csharp
// 修正前
public event EventHandler<object?>? PreviewUpdated;

// 修正後
public event EventHandler<PreviewUpdatedEventArgs>? PreviewUpdated;
```

## 実機テスト手順（ユーザー実行推奨）

### 1. EXE起動
```bash
# エクスプローラーから直接起動（管理者権限禁止）
C:\Users\217216X721451\github\DocOrganizer\release\DocOrganizer.exe
```

### 2. ドラッグ&ドロップテスト手順
1. **サンプルファイル準備**
   - sample/フォルダのJPG/PNG/HEIC/PDFファイルを使用
   - 複数ファイルを選択してドラッグ

2. **左側サムネイル確認**
   - ファイルドロップ後、左側にページサムネイルが表示されることを確認
   - サムネイルの順序変更（ドラッグ&ドロップ）動作確認

3. **右側プレビュー確認（重要）**
   - 左側サムネイルをクリック
   - 右側に高解像度プレビューが表示されることを確認
   - ページ切り替え時にプレビューが正常に更新されることを確認

4. **ドラッグ&ドロップ並び替え + プレビュー更新テスト**
   - 左側でページの順序を変更
   - 変更したページを選択
   - 右側プレビューが正しく更新されることを確認

### 3. 期待される動作
- ✅ 左側サムネイル表示
- ✅ 右側プレビュー表示
- ✅ ページ選択時のプレビュー更新
- ✅ ドラッグ&ドロップ並び替え
- ✅ 並び替え後のプレビュー正常更新

## テスト完了条件
ユーザーが「右側プレビューが正常に表示・更新される」ことを確認したら、この問題は解決済みとする。

## 生成日時
2025-08-18 19:58:26 JST

## 最終更新
2025-08-18 13:47 JST - 全修正完了、EXE生成成功